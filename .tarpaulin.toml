# cargo-tarpaulin config — generates line-coverage report for ratatui-ppalla.
# Run: `cargo tarpaulin --config .tarpaulin.toml`
# Report: docs/coverage/index.html

[default]
skip-clean = true
out = ["Html", "Std"]
output-dir = "docs/coverage"
